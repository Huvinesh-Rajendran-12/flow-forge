# FlowForge Backend Configuration
#
# Option A: Direct Anthropic API
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
#
# Option B: OpenRouter (recommended for failover and cost tracking)
# OPENROUTER_API_KEY=your_openrouter_api_key_here
# ANTHROPIC_BASE_URL=https://openrouter.ai/api
# ANTHROPIC_AUTH_TOKEN=your_openrouter_api_key_here
# ANTHROPIC_API_KEY=
#
# Agent Configuration (optional)
# DEFAULT_MODEL=haiku

# -----------------------------------------------------------------------
# Connector Mode
# -----------------------------------------------------------------------
# "simulator" — use in-memory simulators only (default, no external calls)
# "hybrid"    — use real connectors where credentials are set, else simulate
# "real"      — signals intent to use real services (same routing as hybrid)
# CONNECTOR_MODE=simulator

# -----------------------------------------------------------------------
# Slack (requires Bot Token with channels:manage, chat:write, users:read.email)
# -----------------------------------------------------------------------
# SLACK_BOT_TOKEN=xoxb-your-bot-token

# -----------------------------------------------------------------------
# GitHub (requires token with admin:org, repo scopes, or App installation token)
# -----------------------------------------------------------------------
# GITHUB_TOKEN=ghp_your_token
# GITHUB_ORG=your-org-name

# -----------------------------------------------------------------------
# Jira Cloud (Atlassian API token from https://id.atlassian.com/manage-profile/security)
# -----------------------------------------------------------------------
# JIRA_BASE_URL=https://yourorg.atlassian.net
# JIRA_EMAIL=admin@yourorg.com
# JIRA_API_TOKEN=your_atlassian_api_token
# JIRA_PROJECT_KEY=ONBOARD

# -----------------------------------------------------------------------
# Google Workspace (service account with domain-wide delegation)
# Admin SDK: https://admin.google.com/ac/owl/domainwidedelegation
# -----------------------------------------------------------------------
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}
# GOOGLE_ADMIN_EMAIL=admin@yourdomain.com
# GOOGLE_DOMAIN=yourdomain.com

# -----------------------------------------------------------------------
# HR System (generic internal REST API)
# -----------------------------------------------------------------------
# HR_BASE_URL=https://hr.internal/api
# HR_API_KEY=your_internal_hr_bearer_token
